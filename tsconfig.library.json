{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "declaration": true,
    "rootDir": "${configDir}/src"
    //    "rootDirs": ["${configDir}/src", "${configDir}"],
  }
}
